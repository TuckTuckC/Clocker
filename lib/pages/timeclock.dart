import 'package:flutter/material.dart';

class TimeClock extends StatelessWidget {
  const TimeClock({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Time Clock"))
    );
  }
}